version: 2
updates:
  - package-ecosystem: gradle
    directory: "/"
    schedule:
      interval: daily
    # set limit to 1 as auto-merge seems to fail if more than 1
    open-pull-requests-limit: 1
    assignees:
      - "@navikt/tpts"
    ignore:
    # Trenger ikke lenger å ignorere noen dependencies, vi har oppgradert til jakarta-namespacet og siste versjon av cxf
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: daily
    assignees:
      - "@navikt/tpts"
